<UserControl x:Class="Gaten.Study.TestWpf.PairControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:Gaten.Study.TestWpf"
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="200">
    <!--<UserControl.DataContext>
        <local:PairControlViewModel x:Name="ViewModel" />
    </UserControl.DataContext>-->
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Column="0" Text="{Binding Pair.Symbol}"/>
        <TextBlock Grid.Column="1" Text="{Binding Pair.Price}"/>
    </Grid>
</UserControl>
