<Window x:Class="Gaten.Network.SpeedSearch.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Gaten.Network.SpeedSearch"
        mc:Ignorable="d"
        Title="Speed Search" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="26"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="100"/>
            </Grid.ColumnDefinitions>
                <TextBox x:Name="KeywordTextBox" Grid.Column="0" FontSize="17" VerticalAlignment="Center"/>
            <Button x:Name="SearchButton" Grid.Column="1" FontSize="14" Content="Search" Click="SearchButton_Click"/>
        </Grid>

        <Grid Grid.Row="1">
            <TextBox x:Name="ResultTextBox" IsReadOnly="True" AcceptsReturn="True" AcceptsTab="True" TextWrapping="Wrap"/>
        </Grid>
    </Grid>
</Window>
